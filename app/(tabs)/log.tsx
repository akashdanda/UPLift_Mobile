import { View } from 'react-native'

/**
 * Placeholder for the center “Post” tab. The tab bar intercepts presses and opens
 * /log-workout; this screen is never meant to stay visible.
 */
export default function LogTabScreen() {
  return <View style={{ flex: 1 }} />
}
